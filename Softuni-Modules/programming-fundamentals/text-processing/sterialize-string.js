function solve(str){
    let index = 0; 

for(let i = 0; i < str.length; i++){
    let split = str[i].split("");

    for(let a = 0; a<split.length; a++){
        let currEl = split[a]
        console.log(currEl)
 
        // str.charCodeAt()
        // console.log(`${el}:${index1}/${index2}/${index3}`)
        }
    }


}
solve(["abababa"])